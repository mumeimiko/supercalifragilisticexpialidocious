1. Create a replicaset via declarative method
2. Create a replicaset via imperative  method
3. Create a replicaset with the image $IMAGE with $NUM_REP replicas
4. Create a replicaset with the image $IMAGE with $NUM_REP replicas
5. Create a replicaset with the following Key/Pair Values: $KEY $VALUE
6. Two part question
    - Create a pod named playhouse with the following key pair: Key=environment, Value=prod
    - Create a replicaset that would assorb this pod 